from django.apps import AppConfig


class FieldopsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.fieldops"
    verbose_name = "Agents, territories, visits, targets, mobile sync"
    """Agents, territories, visits, targets, mobile sync."""

from django.apps import AppConfig


class FarmersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.farmers"
    verbose_name = "Farmer, land parcels, crops, livestock, org links"
    """Farmer, land parcels, crops, livestock, org links."""

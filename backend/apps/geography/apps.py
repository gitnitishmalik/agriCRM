from django.apps import AppConfig


class GeographyConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.geography"
    verbose_name = "ref.* — states, districts, blocks, villages, LGD sync"
    """ref.* — states, districts, blocks, villages, LGD sync."""

-r base.txt

# Lint / format / types (Doc 03 §10)
ruff>=0.6
mypy>=1.11
django-stubs[compatible-mypy]>=5.0
djangorestframework-stubs>=3.15

# Tests
pytest>=8.3
pytest-django>=4.9
pytest-cov>=5.0
factory-boy>=3.3
freezegun>=1.5

# Query debugging — used in the Doc 04 §6 N+1 hunt
django-silk>=5.2

# Secret scanning runs via pre-commit
pre-commit>=3.8

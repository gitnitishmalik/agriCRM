# Core
# NOTE: Doc 03 specifies Django 5.0. Pinned to 5.2 LTS here because Django 5.0
# does not support Python 3.13 (5.1 added it) and 5.2 is the current LTS.
Django>=5.2,<5.3
djangorestframework>=3.15,<3.17
django-cors-headers>=4.4
psycopg[binary]>=3.2

# API schema -> generated TypeScript client (Doc 03 §10: contract drift fails the build)
drf-spectacular>=0.27

# Auth. JWT 15min access / 7day rotating refresh + TOTP MFA (Doc 12 §1)
djangorestframework-simplejwt>=5.3
django-otp>=1.5
qrcode>=7.4

# Brute-force lockout: 10 failures / 15 min (Doc 12 §13)
django-axes>=6.5

# Async work: imports, exports, sends, collectors
celery>=5.4,<6
redis>=5.0
django-celery-beat>=2.7

# Config + storage
django-environ>=0.11
django-storages[boto3]>=1.14

# Observability. PII scrubbing is configured in settings, not retrofitted (R12).
sentry-sdk[django]>=2.13

# Serving
gunicorn>=23.0
whitenoise>=6.7
